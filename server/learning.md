--save-dev -> for dependencies like ts, eslint, which we don't need in production
--save or nothing -> npm i express -> for dependencies which we need in production

ts-node -> directly run the ts file, other need to first build, and then run
